apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    run: test
  name: bluered
spec:
  replicas: 2
  selector:
    matchLabels:
      run: blue
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        run: blue
    spec:
      containers:
      - image: nginx
        name: nginx
        volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: blue
      volumes:
      - name: blue
        configMap:
          name: blue
